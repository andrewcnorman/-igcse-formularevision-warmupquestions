% In essence we want the vertical pitch to be such that there are 30 steps.
%
\newlength{\rad@outer@pitch} \setlength{\rad@outer@pitch}{0.0333333\paperheight}

\setbeamersize{text margin left=2\rad@outer@pitch}
\setbeamersize{text margin right=2\rad@outer@pitch}

\setbeamerfont{section in head/foot}{size=\Large}
\setbeamerfont{logo in head/foot}{parent=subsection in head/foot}

\setbeamerfont{progress}{size=\tiny}
\setbeamercolor{progress}{fg=structure.fg!50}

% The School logo
\pgfdeclareimage[height=1.5\rad@outer@pitch]{beamerlogoradley}{CrossedKeysRadleyHorizontalHiResRev-acn}

\mode<presentation>

% Mini frames

\defbeamertemplate*{mini frame}{radley theme}
{%
  \begin{pgfpicture}{0pt}{0pt}{0.1cm}{0.1cm}
    \pgfpathcircle{\pgfpoint{0.05cm}{0.05cm}}{0.05cm}
    \pgfusepath{fill,stroke}
  \end{pgfpicture}%
}
[action]
{
  \setbeamersize{mini frame size=.14cm,mini frame offset=.03cm}
}

\defbeamertemplate*{mini frame in current subsection}{radley theme}
{%
  \begin{pgfpicture}{0pt}{0pt}{0.1cm}{0.1cm}
    \pgfpathcircle{\pgfpoint{0.05cm}{0.05cm}}{0.05cm}
    \pgfusepath{stroke}
  \end{pgfpicture}%
}

\defbeamertemplate*{mini frame in other subsection}{radley theme}[1][50]
{\color{fg!#1!bg}\usebeamertemplate{mini frame in current subsection}}

% Navigation symbols
\defbeamertemplate*{navigation symbols}{radley theme}{}

% Section and subsections in head/foot

\defbeamertemplate*{section in head/foot}{radley theme}
{\insertsectionhead}

\defbeamertemplate*{section in head/foot shaded}{radley theme}[1][50]
{\color{fg!#1!bg}\usebeamertemplate{section in head/foot}}

\defbeamertemplate*{subsection in head/foot}{radley theme}
{\insertsubsectionhead}

\defbeamertemplate*{subsection in head/foot shaded}{radley theme}[1][50]
{\color{fg!#1!bg}\usebeamertemplate{subsection in head/foot}}

\defbeamertemplate*{subsubsection in head/foot}{radley theme}
{\insertsubsubsectionhead}

\defbeamertemplate*{subsubsection in head/foot shaded}{radley theme}[1][50]
{\color{fg!#1!bg}\usebeamertemplate{subsubsection in head/foot}}

% background

\defbeamertemplate*{background}{radley theme}
{}

% Headline and footline for a NORMAL SLIDE:

\defbeamertemplate*{frametitle}{radley theme}
{%
  \vskip-1pt % HACK: Removes a bright line at the top of the page.
  \begin{beamercolorbox}[wd=\paperwidth,ht=4.5\rad@outer@pitch,dp=\rad@outer@pitch,leftskip=2\rad@outer@pitch,sep=0pt]{palette primary}%
    \begin{beamercolorbox}[ht=0pt,sep=0pt]{}%
      \usebeamerfont{frametitle}\insertframetitle
    \end{beamercolorbox}
  \end{beamercolorbox}%
  \nointerlineskip
%  \begin{beamercolorbox}[wd=\paperwidth,dp=0.5\rad@outer@pitch]{lower separation line head}%
%  \end{beamercolorbox}%
}

\defbeamertemplate*{footprogress}{radley theme}
{%
  \usebeamerfont{progress}\usebeamercolor[fg]{progress}\insertframenumber\ of \inserttotalframenumber
}

% Frame title continuations, default

\defbeamertemplate*{frametitle continuation}{radley theme}{\insertcontinuationcountroman}

%\defbeamertemplate*{footprogress}{radley theme}
%{%
%\begin{tikzpicture}[x=\rad@outer@pitch,y=\rad@outer@pitch]
%   \draw[step=0.5,gray,very thin] (-1.4,-1.4) grid (1.4,1.4); 
%   \draw (-1.5,0) -- (1.5,0);
%   \draw (0,-1.5) -- (0,1.5);
%   \draw (0,0) circle (1); \draw (0.3,0) arc (0:30:0.3);
%\end{tikzpicture}

 % \insertframenumber\ of \inserttotalframenumber
%}

\defbeamertemplate*{footline}{radley theme}
{
  \ifx \insertframetitle \@empty
    % There was no frame title
  \else
    % There was a frame title, typeset the counterpart to the frame title (this is a hack).
    \begin{beamercolorbox}[sep=0.25\rad@outer@pitch,leftskip=0.5\rad@outer@pitch,rightskip=0.5\rad@outer@pitch]{palette secondary}%
      \begin{minipage}{0.2\textwidth}
\hbox{\usebeamertemplate{footprogress}}
\vspace{1em}
\end{minipage}
      \hfill
      \usebeamerfont{logo in head/foot}\insertlogo
    \end{beamercolorbox}%
  \fi
}

% Sidebar

\defbeamertemplate*{sidebar left}{radley theme}
{}

\defbeamertemplate*{sidebar right}{radley theme}
{}

\defbeamertemplate*{sidebar canvas left}{radley theme}
{}

\defbeamertemplate*{sidebar canvas right}{radley theme}
{}

% Notes

\defbeamertemplate*{note page}{radley theme}
{%
  {%
    \scriptsize
    \insertvrule{.25\paperheight}{white!90!black}
    \vskip-.25\paperheight
    \nointerlineskip
    \vbox{
      \hfill\insertslideintonotes{0.25}\hskip-\Gm@rmargin\hskip0pt%
      \vskip-0.25\paperheight%
      \nointerlineskip
      \begin{pgfpicture}{0cm}{0cm}{0cm}{0cm}
        \begin{pgflowlevelscope}{\pgftransformrotate{90}}
          {\pgftransformshift{\pgfpoint{-2cm}{0.2cm}}%
          \pgftext[base,left]{\footnotesize\the\year-\ifnum\month<10\relax0\fi\the\month-\ifnum\day<10\relax0\fi\the\day}}
        \end{pgflowlevelscope}
      \end{pgfpicture}}
    \nointerlineskip
    \vbox to .25\paperheight{\vskip0.5em
      \hbox{\insertshorttitle[width=8cm]}%
      \setbox\beamer@tempbox=\hbox{\insertsection}%
      \hbox{\ifdim\wd\beamer@tempbox>1pt{\hskip4pt\raise3pt\hbox{\vrule
            width0.4pt height7pt\vrule width 9pt
            height0.4pt}}\hskip1pt\hbox{\begin{minipage}[t]{7.5cm}\def\breakhere{}\insertsection\end{minipage}}\fi%
      }%
      \setbox\beamer@tempbox=\hbox{\insertsubsection}%
      \hbox{\ifdim\wd\beamer@tempbox>1pt{\hskip17.4pt\raise3pt\hbox{\vrule
            width0.4pt height7pt\vrule width 9pt
            height0.4pt}}\hskip1pt\hbox{\begin{minipage}[t]{7.5cm}\def\breakhere{}\insertsubsection\end{minipage}}\fi%
      }%
      \setbox\beamer@tempbox=\hbox{\insertshortframetitle}%
      \hbox{\ifdim\wd\beamer@tempbox>1pt{\hskip30.8pt\raise3pt\hbox{\vrule
            width0.4pt height7pt\vrule width 9pt
            height0.4pt}}\hskip1pt\hbox{\insertshortframetitle[width=7cm]}\fi%
      }%
      \vfil}%
  }%
  \vskip.25em
  \nointerlineskip
  \insertnote
}

\mode<all>

\ifbeamertemplateempty{logo}{%
   \setbeamertemplate{logo}{%
      \pgfuseimage{beamerlogoradley}
   }
}{%
   % Do nothing if logo is set.
}
